# Claude Code / agent artifacts privados
# settings.local.json guarda permissões e possíveis tokens da máquina local — não versionar.
# skills/ e settings.json (compartilhados) permanecem versionados.
.claude/settings.local.json
.claude/*.local.json
.claude/.credentials.json

# OS
.DS_Store

# Node / Next.js
node_modules/
.next/
out/
build/
dist/
next-env.d.ts
*.tsbuildinfo
coverage/
playwright-report/
test-results/
.env*

# Outreach local (contatos reais mantidos fora do repositório público)
*.local.md

# Deck: PDF é gerado localmente a partir de deck/deck.html — não versionar o binário
docs/outreach/rubinot-statistics-proposta.pdf
